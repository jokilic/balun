import 'package:flutter/material.dart';

class MatchFormationsSection extends StatelessWidget {
  @override
  Widget build(BuildContext context) => const Placeholder(
        color: Colors.cyan,
      );
}
