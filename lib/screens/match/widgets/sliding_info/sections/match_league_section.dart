import 'package:flutter/material.dart';

class MatchLeagueSection extends StatelessWidget {
  @override
  Widget build(BuildContext context) => const Placeholder(
        color: Colors.green,
      );
}
