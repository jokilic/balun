import 'package:flutter/material.dart';

class MatchHeadToHeadSection extends StatelessWidget {
  @override
  Widget build(BuildContext context) => const Placeholder(
        color: Colors.lime,
      );
}
